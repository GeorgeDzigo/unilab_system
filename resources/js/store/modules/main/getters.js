export default {

    /**
     * Get card id data.
     *
     * @param state
     */
    cardId: state => state.cardId,

    /**
     *
     * @param state
     * @returns {default.personalCardImages|(function(*))|state.personalCardImages|{}}
     */
    personalCardImages: state => state.personalCardImages

}
